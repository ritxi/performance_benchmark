require 'spec_helper'

describe PerformanceBenchmark do
  it 'has a version number' do
    expect(PerformanceBenchmark::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
